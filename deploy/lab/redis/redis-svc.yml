---
apiVersion: v1
kind: Service
metadata:
  name: redis-svc
  namespace: redis
  labels:
    app: redis
spec:
  ports:
    - port: 6379
      name: redis
    - port: 16379
      name: cluster-bus
  clusterIP: None
  selector:
    app: redis
